FIXME: Some introduction here

Example launch lines for msrtahealer:

Receiver:
gst-launch-0.10 -v udpsrc \
    caps="application/x-rtp, payload=114, clock-rate=16000" \
    ! msrtahealer ! alsasink sync=false

Sender:
gst-launch-0.10 -v audiotestsrc samplesperbuffer=320 is-live=true \
    ! msenc_rta16 bitrate=29000 ! rtprtaudiopay pt=114 ! udpsink

